# SQLite database used by the POS backend
DATABASE_URL="file:./prisma/pos.db"

# JWT — change this to a long random string in production
JWT_SECRET="change_this_to_a_very_long_random_secret_key"

# Server port
PORT=3000
